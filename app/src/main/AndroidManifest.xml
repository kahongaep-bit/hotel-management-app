<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hotelmanagementsystem">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.HotelManagementSystem">

        <!-- SKRINI YA KWANZA KUANZA (LOGIN) -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- SKRINI ZINGINE ZA MFUMO -->
        <activity android:name=".MainActivity" android:exported="false" />
        <activity android:name=".CashierActivity" android:exported="false" />
        <activity android:name=".FinancePriceActivity" android:exported="false" />
        <activity android:name=".ProcurementActivity" android:exported="false" />
        <activity android:name=".ManagerRequisitionActivity" android:exported="false" />
        <activity android:name=".ProductionManagerActivity" android:exported="false" />
        <activity android:name=".PrincipalDashboardActivity" android:exported="false" />
        <activity android:name=".BartenderActivity" android:exported="false" />
        <activity android:name=".KitchenActivity" android:exported="false" />

    </application>
</manifest>